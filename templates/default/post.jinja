{% extends "default/base.jinja" %}

{% block title %}Add link{% endblock %}
{% block pagetitle %}Add a link - {% endblock %}
{% block content %}
	{% with messages = get_flashed_messages() %}
		{% if messages %}
			{% for msg in messages %}
				<div class="error-message">{{msg}}</div>
			{% endfor %}
		{% endif %}
	{% endwith %}
	<form action="{{form.target}}" method="POST">
		{{ form.hidden_tag() }}
		<div class="form-group">
			{{ form.title.label }}
			{{ form.title()}}
		</div>
		<div class="form-group">
			{{ form.url.label }}
			{{ form.url() }}
		</div>
		<div class="form-group">
			{{ form.desc.label }}
			{{ form.desc(height=10) }}
		</div>
		<div class="form-group">
			{{ form.tags.label }}
			{{ form.tags() }}
		</div>

		<input type="submit" name="title" value="Post">
	</form>
{% endblock %}